# SQL Basics

This directory covers the **fundamental SQL querying patterns**
required for data analysis, reporting, and analytical workflows.

It is designed to build a strong foundation for:
- Exploratory Data Analysis (EDA)
- Business and analytical reporting
- Data preprocessing before modeling

본 폴더는 SQL의 가장 기본적인 조회(Query) 문법을 다루며,  
데이터 분석과 리포팅에 필수적인 SQL 사고방식을 체계적으로 정리하는 것을 목표로 합니다.

---

## 🎯 Learning Objectives

- Understand how to retrieve data using SQL queries
- Filter, sort, and limit query results effectively
- Build readable and logically structured SQL statements
- Prepare for aggregation, joins, and analytical SQL patterns

---

## 📂 Structure & Progress

Each file focuses on one core SQL concept.  
Files are completed incrementally with daily commits.

각 파일은 하나의 핵심 SQL 개념을 다루며,  
일일 학습 단위로 순차적으로 완성됩니다.

### ✅ Completed

- `01_select.sql`  
  Basic data retrieval using SELECT  
  기본 데이터 조회 (컬럼 선택, 전체 조회)

- `02_where.sql`  
  Filtering rows with conditions  
  조건을 활용한 데이터 필터링 (비교 연산자, AND/OR)

- `03_order_by.sql`  
  Sorting query results  
  결과 정렬 (ASC/DESC, 다중 컬럼 정렬)

- `04_limit.sql`  
  Limiting result size  
  조회 결과 제한 (LIMIT, OFFSET, Top-N 분석)

---

## 🧠 Why SQL Basics Matter

SQL basics are the foundation of:
- Data exploration and sanity checks
- Feature extraction for machine learning
- Reporting and dashboard queries
- Efficient communication with databases

Without a solid understanding of filtering, sorting, and limiting,
it is difficult to write reliable and meaningful analytical queries.

---

## 📌 학습 요약 (한국어)

- SELECT를 통한 데이터 조회의 기본 구조 이해
- WHERE 조건을 활용한 필터링 로직 습득
- ORDER BY를 통한 분석 친화적 정렬 구현
- LIMIT/OFFSET을 활용한 샘플링 및 Top-N 분석
- 이후 GROUP BY, JOIN, WINDOW FUNCTION으로 확장 준비

---

## 🚧 Status

**Completed – SQL Basics (Day 1–4)**

This section establishes the core SQL querying foundation  
and serves as the entry point for advanced analytical SQL topics.

본 단계는 SQL 기초 학습 단계로,  
이후 집계(GROUP BY), 조인(JOIN), 윈도우 함수로 확장됩니다.